// exports a styled span
// use a different font than the site's default
// supports an optional 'color' prop to set the color
// supports an optional 'big' prop to make it twice as big
// supports an optional 'bold' prop to make the text bolder
// supports an optional 'italic' prop to make the text italic
